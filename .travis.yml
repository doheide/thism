group: travis_latest

language: cpp

compiler: gcc

install: 
  - sudo apt-get install -qq g++-6
  - sudo update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-6 90

script:
  - qmake
  - make


before_install:
    - eval "${MATRIX_EVAL}"
